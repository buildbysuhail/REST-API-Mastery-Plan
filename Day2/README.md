### To Do Or What i done:

## CRUD API for Users (MongoDB)
> Minimum User fields (keep it simple):
  - name
  - email
  - age(optional)

> Endpoints
  - POST /users
  - GET /users
  - GET /users/:id
  - PUT or PATCH /users/:id
  - DELETE /users/:id

### Tech Stack used:
* Node.js, Express.js
* MongoDB
* dotenv
